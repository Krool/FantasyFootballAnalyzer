# Sentry client key (DSN). Public by design: it can only send events, not read
# them, so it's safe to commit and ship in the browser bundle. Read at build
# time by src/utils/sentry.ts; gated on PROD so dev builds stay dark.
VITE_SENTRY_DSN=https://64939d117b1bd5c2ec1fc2fb5cbc09e5@o4511568223797248.ingest.us.sentry.io/4511568227860480
